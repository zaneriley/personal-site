# Acceptance-Gate Bypass Fixtures

`pass/` contains workflows that must pass the checker. `fail/` contains one
known-bad bypass per directory. Fixture verification uses `shared/run` unless a
case provides its own `run` file.
