var __wpo = {"assets":{"main":["/dist/gt-america-standard-light.519bb1ea6b8403b3a6043e087888a6aa.woff","/dist/maria.3e5a8cfb4092e0eed7d651cb3c31a9cb.woff","/dist/angellist.c9abbf0f18b2b4eb5996114f746aba08.svg","/dist/codepen.576ad3c75c6ecd1f6a9e744b71741c1f.svg","/dist/dribbble.597b43460140fd6e9fc132f64f132ddf.svg","/dist/github.caf25e0cd9792d26636aaa41cd45823a.svg","/dist/linkedin.1edad69e4977b0d2451341339f3f2f3c.svg","/dist/twitter.946a094e84d78ffe607b59cd41876306.svg","/dist/sketch-time-lapse.5c806fc25e26dddb00296c6967979d64.mp4","/dist/sketch-time-lapse.8d368507abe5f87e270d27857212759d.webm","/dist/user-test.be6954a390c9318d1ee60487ace90735.mp4","/dist/user-test.a3807fd509e77be96dd74b26797ca97e.webm","/dist/add-to-cart.0a37e2d8a8163373587d21d5acaa0a5c.svg","/dist/callout-panel.ccb872d124b62bcdfa05f163a9a2b3d7.svg","/dist/class-wireframe.7d53fef58f76a6329b9700f78898adf4.svg","/dist/flow-all-classes.de7534d7243dd8f2b6a8ec1ebec18cc3.svg","/dist/flow-all-kits.1e51c51c2a91baddde14aeb5d4aaec02.svg","/dist/flow-arrow-right.284bca6ddd98a3fb565cf33308d4eec6.svg","/dist/flow-single-Kit.d015dee3ef01b17719d3e0a9b65fe25a.svg","/dist/flow-single-class.4e5f94b1bc3584b45f5fb8df71b6a45a.svg","/dist/galaxy-2880w.74bb5efeb1e8da03c9387c505e7422dd.jpg","/dist/kit-pdp-desktop-after-1440w.095fa41e8b92663a3429953cf3a27ed9.jpg","/dist/kit-pdp-desktop-after-20w.9b227e0e152ad448a84da3edd5388fc4.jpg","/dist/kit-pdp-desktop-after-2796w.3018079624d8f89d65e7be0d10392119.jpg","/dist/kit-pdp-desktop-after-320w.a05cd804900a7fa50335269a90c0e75b.jpg","/dist/kit-pdp-desktop-after-494w.2c2cc570dce802b8fefe5fa4a85e8272.jpg","/dist/kit-pdp-desktop-after-960w.3de6fbcf6a8e21ef445e219c0d1b976d.jpg","/dist/kit-pdp-desktop-before-1440w.04a13868e72aa9d8523c0196fa0973fe.jpg","/dist/kit-pdp-desktop-before-20w.e0229f6567781ef10998cdfb919fa11f.jpg","/dist/kit-pdp-desktop-before-2796w.47b52469f5a3bec56b248305b2a5854f.jpg","/dist/kit-pdp-desktop-before-320w.eb20041b2a95589e492496da6a159bcf.jpg","/dist/kit-pdp-desktop-before-494w.305a597fd3c1c9b57d3d18198c44f708.jpg","/dist/kit-pdp-desktop-before-960w.60bd108e587c89c75206d02fd8794863.jpg","/dist/kit-wireframe.1bb0e80e701488fa5eb60b4ba5c596c1.svg","/dist/sketch-text-sizes-2560w.6d6e2c2f72bf40cb6134c4acdcc19b2e.jpg","/dist/sketch-time-lapse-3360w.bf707de170d90ce0b18b6dc5d23c0ea4.jpg","/dist/styleguide-1680w.a4afe5301ac79135c3e94cb438d7ebc4.png","/dist/user-test-20w.281fa937ae02bad95421034f765b6d27.jpg","/dist/user-test-928w.0cdade50e452fe089d00efd833ee0869.jpg","/dist/user-test-result@2x.831f8a10469f83072d7689da3d3dd358.jpg","/dist/0.9be9d7263ce168ce412d.js","/dist/1.f3496a0fc2959eb01ee2.js","/dist/2.99ce09bd07363a4aae3a.js","/dist/main.af55cc6a762702f0cdcd.js"],"additional":[],"optional":[]},"externals":[],"hashesMap":{"b35228d639e51dd8e8b78df50782c4b973121e5d":"/dist/gt-america-standard-light.519bb1ea6b8403b3a6043e087888a6aa.woff","9425efd25a8d34368fda6fda52e6d5bfe8a672c2":"/dist/maria.3e5a8cfb4092e0eed7d651cb3c31a9cb.woff","d69dace3a52f1be7ab25f7aec9fb2b63798a8294":"/dist/angellist.c9abbf0f18b2b4eb5996114f746aba08.svg","8801a567d6f11e06564c76e1eda3a35163d93968":"/dist/codepen.576ad3c75c6ecd1f6a9e744b71741c1f.svg","c33dec75c69a0cbac73a9263cc4665b173ce431c":"/dist/dribbble.597b43460140fd6e9fc132f64f132ddf.svg","69269c78ae87474285cd668fe83e0d3606d0d8a7":"/dist/github.caf25e0cd9792d26636aaa41cd45823a.svg","c261ef9a7c3a338389f7390681f066ad0d760fdb":"/dist/linkedin.1edad69e4977b0d2451341339f3f2f3c.svg","e87e3507b48f0a328079f96afc41c4b91b1b0409":"/dist/twitter.946a094e84d78ffe607b59cd41876306.svg","10ef9e8ecd55e6111677aa035cf71ba8960bfb36":"/dist/sketch-time-lapse.5c806fc25e26dddb00296c6967979d64.mp4","79af297470a07159322fbdcc72251cd598897bd6":"/dist/sketch-time-lapse.8d368507abe5f87e270d27857212759d.webm","724e13cf08fda177546812c8cda0a5a585da190f":"/dist/user-test.be6954a390c9318d1ee60487ace90735.mp4","aa1ed5b22f5a6d8e6343127d88d895a173889c73":"/dist/user-test.a3807fd509e77be96dd74b26797ca97e.webm","5b79c1434c80919243646069f8725fe7c707bfbc":"/dist/add-to-cart.0a37e2d8a8163373587d21d5acaa0a5c.svg","8a081071d408cb7ff5e7706141e7fff7c0542d8e":"/dist/callout-panel.ccb872d124b62bcdfa05f163a9a2b3d7.svg","8f2167d1dd835afc0723eac27ad3e367be0b096a":"/dist/class-wireframe.7d53fef58f76a6329b9700f78898adf4.svg","4df54877ca11dd55cdf611e07327c9ca42acf76c":"/dist/flow-all-classes.de7534d7243dd8f2b6a8ec1ebec18cc3.svg","5ec0cb04a7db5daa7a991b5eea9e6b03f92b7679":"/dist/flow-all-kits.1e51c51c2a91baddde14aeb5d4aaec02.svg","83adf1756723ce569a74136973f3281b91af2a4a":"/dist/flow-arrow-right.284bca6ddd98a3fb565cf33308d4eec6.svg","a399f327df9f1040c83ea0a3874a287b50b623f5":"/dist/flow-single-Kit.d015dee3ef01b17719d3e0a9b65fe25a.svg","d806efe6e6531fc3fd2ef86a6d295de822902ec5":"/dist/flow-single-class.4e5f94b1bc3584b45f5fb8df71b6a45a.svg","29ef9c79c46bb604ddbca35a5f061ec939f5a9cb":"/dist/galaxy-2880w.74bb5efeb1e8da03c9387c505e7422dd.jpg","c21d827be254f6f7f150e42c4eae1560b2341546":"/dist/kit-pdp-desktop-after-1440w.095fa41e8b92663a3429953cf3a27ed9.jpg","5da94add58e787cb27340153204857922501d6af":"/dist/kit-pdp-desktop-after-20w.9b227e0e152ad448a84da3edd5388fc4.jpg","86fa5234ce4474464a460db454c013f26f29a1d3":"/dist/kit-pdp-desktop-after-2796w.3018079624d8f89d65e7be0d10392119.jpg","ded5e7ed841d028c01d7e5448d2e18ded633aac1":"/dist/kit-pdp-desktop-after-320w.a05cd804900a7fa50335269a90c0e75b.jpg","dcc71a2308008594de1c898176833e16653faa6e":"/dist/kit-pdp-desktop-after-494w.2c2cc570dce802b8fefe5fa4a85e8272.jpg","beae01e0ab6b9d69e68218df849b30f135a0f39c":"/dist/kit-pdp-desktop-after-960w.3de6fbcf6a8e21ef445e219c0d1b976d.jpg","bf5398b40f08b783840a7e947e9b627dc446ecac":"/dist/kit-pdp-desktop-before-1440w.04a13868e72aa9d8523c0196fa0973fe.jpg","6192c42705c640219cb8e19248e0e01e06864041":"/dist/kit-pdp-desktop-before-20w.e0229f6567781ef10998cdfb919fa11f.jpg","f5e10dd3a406cf44415dc72542e3202957a5109c":"/dist/kit-pdp-desktop-before-2796w.47b52469f5a3bec56b248305b2a5854f.jpg","0a48a17c8290dae8dfeff6e972df29c2343d82a2":"/dist/kit-pdp-desktop-before-320w.eb20041b2a95589e492496da6a159bcf.jpg","ba4a6c620cfeaa201fcb797057fbb775c2ba1fc6":"/dist/kit-pdp-desktop-before-494w.305a597fd3c1c9b57d3d18198c44f708.jpg","ade36f6fce29462b2914c6ee0e236842af3bcceb":"/dist/kit-pdp-desktop-before-960w.60bd108e587c89c75206d02fd8794863.jpg","8d1a7cd96a8cec4307f735fed81fe39061714577":"/dist/kit-wireframe.1bb0e80e701488fa5eb60b4ba5c596c1.svg","401bda083d6a7976c98181cb3faa49c7d2ba2a1f":"/dist/sketch-text-sizes-2560w.6d6e2c2f72bf40cb6134c4acdcc19b2e.jpg","f4f9ed2ec3b77b19a3f60cf235d656c00f4104b3":"/dist/sketch-time-lapse-3360w.bf707de170d90ce0b18b6dc5d23c0ea4.jpg","6b47230a85778934b5792ef10cb076dd72fd2d25":"/dist/styleguide-1680w.a4afe5301ac79135c3e94cb438d7ebc4.png","45b8a45b807b8b9a4cdec36db59dff9edaf1aa12":"/dist/user-test-20w.281fa937ae02bad95421034f765b6d27.jpg","350ac8866bc05829cf593d3182db3ffb6541759c":"/dist/user-test-928w.0cdade50e452fe089d00efd833ee0869.jpg","4a95aa22f6202980593cb27f4f462da0d2f28233":"/dist/user-test-result@2x.831f8a10469f83072d7689da3d3dd358.jpg","ac0e765ef414c19fe18e1b70a34559163d5e6fa6":"/dist/0.9be9d7263ce168ce412d.js","3733ce32b8abfd41f9abbd3778280cf1f0d575ad":"/dist/1.f3496a0fc2959eb01ee2.js","3f0c7b520bfb8eeb57ef5e62844e9f5b63583024":"/dist/2.99ce09bd07363a4aae3a.js","bdf49120709d5f450e7cf54e38f145af1dec27b0":"/dist/main.af55cc6a762702f0cdcd.js"},"navigateFallbackURL":"../public/index.html","strategy":"changed","responseStrategy":"cache-first","version":"3/13/2017, 3:00:44 PM","name":"webpack-offline","pluginVersion":"4.6.1","relativePaths":true};

/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId])
/******/ 			return installedModules[moduleId].exports;
/******/
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// identity function for calling harmony imports with the correct context
/******/ 	__webpack_require__.i = function(value) { return value; };
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, {
/******/ 				configurable: false,
/******/ 				enumerable: true,
/******/ 				get: getter
/******/ 			});
/******/ 		}
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 1);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, exports) {

/* eslint-disable prefer-const, no-var */

// inspired from
// https://googlechrome.github.io/samples/service-worker/custom-offline-page/

function createCacheBustedRequest(url) {
  let request = new Request(url, { cache: "reload" })
  // See https://fetch.spec.whatwg.org/#concept-request-mode
  // This is not yet supported in Chrome as of M48, so we need to explicitly
  // check to see if the cache: "reload" option had any effect.
  if ("cache" in request) {
    return request
  }

  // If { cache: "reload" } didn't have any effect, append a cache-busting URL
  // parameter instead.
  var bustedUrl = new URL(url, self.location.href)
  bustedUrl.search += (bustedUrl.search ? "&" : "") + "cachebust=" + Date.now()
  return new Request(bustedUrl)
}

self.addEventListener("fetch", event => {
  // We only want to call event.respondWith() if this is a navigation request
  // for an HTML page.
  if (
    event.request.mode === "navigate" ||
    // request.mode of "navigate" is unfortunately not supported in Chrome
    // versions older than 49, so we need to include a less precise fallback,
    // which checks for a GET request with an Accept: text/html header.
    (
      event.request.method === "GET" &&
      event.request.headers.get("accept").indexOf("text/html") > -1
      // event.request.headers.get("accept").includes("text/html")
    )
  ) {
    console.log("Handling fetch event for", event.request.url)
    event.respondWith(
      fetch(createCacheBustedRequest(event.request.url)).catch(error => {
        // The catch is only triggered if fetch() throws an exception, which
        // will most likely happen due to the server being unreachable.
        // If fetch() returns a valid HTTP response with an response code in
        // the 4xx or 5xx range, the catch() will NOT be called.
        // If you need custom handling for 4xx or 5xx errors, see
        // https://github.com/GoogleChrome/samples/tree/gh-pages/
        // service-worker/fallback-response
        console.log("Fetch failed; returning offline page instead.", error)
        return caches.match("index.html")
      })
    )
  }

  // If our if() condition is false, then this fetch handler won"t intercept
  // the request.
  // If there are any other fetch handlers registered, they will get a chance
  // to call event.respondWith().
  // If no fetch handlers call event.respondWith(), the request will be
  // handled by the browser as if there were no service worker involvement.
})

/***/ }),
/* 1 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

      

if (typeof DEBUG === 'undefined') {
  var DEBUG = false;
}

function WebpackServiceWorker(params, helpers) {
  var loaders = helpers.loaders;
  var cacheMaps = helpers.cacheMaps;

  var strategy = params.strategy;
  var responseStrategy = params.responseStrategy;

  var assets = params.assets;
  var loadersMap = params.loaders || {};

  var hashesMap = params.hashesMap;
  var externals = params.externals;

  // Not used yet
  // const alwaysRevalidate = params.alwaysRevalidate;
  // const ignoreSearch = params.ignoreSearch;
  // const preferOnline = params.preferOnline;

  var CACHE_PREFIX = params.name;
  var CACHE_TAG = params.version;
  var CACHE_NAME = CACHE_PREFIX + ':' + CACHE_TAG;

  var STORED_DATA_KEY = '__offline_webpack__data';

  mapAssets();

  var allAssets = [].concat(assets.main, assets.additional, assets.optional);
  var navigateFallbackURL = params.navigateFallbackURL;

  self.addEventListener('install', function (event) {
    console.log('[SW]:', 'Install event');

    var installing = undefined;

    if (strategy === 'changed') {
      installing = cacheChanged('main');
    } else {
      installing = cacheAssets('main');
    }

    event.waitUntil(installing);
  });

  self.addEventListener('activate', function (event) {
    console.log('[SW]:', 'Activate event');

    var activation = cacheAdditional();

    // Delete all assets which name starts with CACHE_PREFIX and
    // is not current cache (CACHE_NAME)
    activation = activation.then(storeCacheData);
    activation = activation.then(deleteObsolete);
    activation = activation.then(function () {
      if (self.clients && self.clients.claim) {
        return self.clients.claim();
      }
    });

    event.waitUntil(activation);
  });

  function cacheAdditional() {
    if (!assets.additional.length) {
      return Promise.resolve();
    }

    if (DEBUG) {
      console.log('[SW]:', 'Caching additional');
    }

    var operation = undefined;

    if (strategy === 'changed') {
      operation = cacheChanged('additional');
    } else {
      operation = cacheAssets('additional');
    }

    // Ignore fail of `additional` cache section
    return operation['catch'](function (e) {
      console.error('[SW]:', 'Cache section `additional` failed to load');
    });
  }

  function cacheAssets(section) {
    var batch = assets[section];

    return caches.open(CACHE_NAME).then(function (cache) {
      return addAllNormalized(cache, batch, {
        bust: params.version,
        request: params.prefetchRequest
      });
    }).then(function () {
      logGroup('Cached assets: ' + section, batch);
    })['catch'](function (e) {
      console.error(e);
      throw e;
    });
  }

  function cacheChanged(section) {
    return getLastCache().then(function (args) {
      if (!args) {
        return cacheAssets(section);
      }

      var lastCache = args[0];
      var lastKeys = args[1];
      var lastData = args[2];

      var lastMap = lastData.hashmap;
      var lastVersion = lastData.version;

      if (!lastData.hashmap || lastVersion === params.version) {
        return cacheAssets(section);
      }

      var lastHashedAssets = Object.keys(lastMap).map(function (hash) {
        return lastMap[hash];
      });

      var lastUrls = lastKeys.map(function (req) {
        var url = new URL(req.url);
        url.search = '';

        return url.toString();
      });

      var sectionAssets = assets[section];
      var moved = [];
      var changed = sectionAssets.filter(function (url) {
        if (lastUrls.indexOf(url) === -1 || lastHashedAssets.indexOf(url) === -1) {
          return true;
        }

        return false;
      });

      Object.keys(hashesMap).forEach(function (hash) {
        var asset = hashesMap[hash];

        // Return if not in sectionAssets or in changed or moved array
        if (sectionAssets.indexOf(asset) === -1 || changed.indexOf(asset) !== -1 || moved.indexOf(asset) !== -1) return;

        var lastAsset = lastMap[hash];

        if (lastAsset && lastUrls.indexOf(lastAsset) !== -1) {
          moved.push([lastAsset, asset]);
        } else {
          changed.push(asset);
        }
      });

      logGroup('Changed assets: ' + section, changed);
      logGroup('Moved assets: ' + section, moved);

      var movedResponses = Promise.all(moved.map(function (pair) {
        return lastCache.match(pair[0]).then(function (response) {
          return [pair[1], response];
        });
      }));

      return caches.open(CACHE_NAME).then(function (cache) {
        var move = movedResponses.then(function (responses) {
          return Promise.all(responses.map(function (pair) {
            return cache.put(pair[0], pair[1]);
          }));
        });

        return Promise.all([move, addAllNormalized(cache, changed, {
          bust: params.version,
          request: params.prefetchRequest
        })]);
      });
    });
  }

  function deleteObsolete() {
    return caches.keys().then(function (keys) {
      var all = keys.map(function (key) {
        if (key.indexOf(CACHE_PREFIX) !== 0 || key.indexOf(CACHE_NAME) === 0) return;

        console.log('[SW]:', 'Delete cache:', key);
        return caches['delete'](key);
      });

      return Promise.all(all);
    });
  }

  function getLastCache() {
    return caches.keys().then(function (keys) {
      var index = keys.length;
      var key = undefined;

      while (index--) {
        key = keys[index];

        if (key.indexOf(CACHE_PREFIX) === 0) {
          break;
        }
      }

      if (!key) return;

      var cache = undefined;

      return caches.open(key).then(function (_cache) {
        cache = _cache;
        return _cache.match(new URL(STORED_DATA_KEY, location).toString());
      }).then(function (response) {
        if (!response) return;

        return Promise.all([cache, cache.keys(), response.json()]);
      });
    });
  }

  function storeCacheData() {
    return caches.open(CACHE_NAME).then(function (cache) {
      var data = new Response(JSON.stringify({
        version: params.version,
        hashmap: hashesMap
      }));

      return cache.put(new URL(STORED_DATA_KEY, location).toString(), data);
    });
  }

  self.addEventListener('fetch', function (event) {
    var requestUrl = event.request.url;
    var url = new URL(requestUrl);
    var urlString = undefined;

    if (externals.indexOf(requestUrl) !== -1) {
      urlString = requestUrl;
    } else {
      url.search = '';
      urlString = url.toString();
    }

    // Handle only GET requests
    var isGET = event.request.method === 'GET';
    var assetMatches = allAssets.indexOf(urlString) !== -1;
    var cacheUrl = urlString;

    if (!assetMatches) {
      var cacheRewrite = matchCacheMap(event.request);

      if (cacheRewrite) {
        cacheUrl = cacheRewrite;
        assetMatches = true;
      }
    }

    if (!assetMatches && isGET) {
      // If isn't a cached asset and is a navigation request,
      // fallback to navigateFallbackURL if available
      if (navigateFallbackURL && isNavigateRequest(event.request)) {
        event.respondWith(handleNavigateFallback(fetch(event.request)));

        return;
      }
    }

    if (!assetMatches || !isGET) {
      // Fix for https://twitter.com/wanderview/status/696819243262873600
      if (url.origin !== location.origin && navigator.userAgent.indexOf('Firefox/44.') !== -1) {
        event.respondWith(fetch(event.request));
      }

      return;
    }

    // Logic of caching / fetching is here
    // * urlString -- url to match from the CACHE_NAME
    // * event.request -- original Request to perform fetch() if necessary
    var resource = undefined;

    if (responseStrategy === "network-first") {
      resource = networkFirstResponse(event, urlString, cacheUrl);
    }
    // "cache-first"
    // (responseStrategy has been validated before)
    else {
        resource = cacheFirstResponse(event, urlString, cacheUrl);
      }

    if (navigateFallbackURL && isNavigateRequest(event.request)) {
      resource = handleNavigateFallback(resource);
    }

    event.respondWith(resource);
  });

  self.addEventListener('message', function (e) {
    var data = e.data;
    if (!data) return;

    switch (data.action) {
      case 'skipWaiting':
        {
          if (self.skipWaiting) self.skipWaiting();
        }break;
    }
  });

  function cacheFirstResponse(event, urlString, cacheUrl) {
    return cachesMatch(cacheUrl, CACHE_NAME).then(function (response) {
      if (response) {
        if (DEBUG) {
          console.log('[SW]:', 'URL [' + cacheUrl + '](' + urlString + ') from cache');
        }

        return response;
      }

      // Load and cache known assets
      var fetching = fetch(event.request).then(function (response) {
        if (!response.ok) {
          if (DEBUG) {
            console.log('[SW]:', 'URL [' + urlString + '] wrong response: [' + response.status + '] ' + response.type);
          }

          return response;
        }

        if (DEBUG) {
          console.log('[SW]:', 'URL [' + urlString + '] from network');
        }

        if (cacheUrl === urlString) {
          (function () {
            var responseClone = response.clone();

            caches.open(CACHE_NAME).then(function (cache) {
              return cache.put(urlString, responseClone);
            }).then(function () {
              console.log('[SW]:', 'Cache asset: ' + urlString);
            });
          })();
        }

        return response;
      });

      return fetching;
    });
  }

  function networkFirstResponse(event, urlString, cacheUrl) {
    return fetch(event.request).then(function (response) {
      if (response.ok) {
        if (DEBUG) {
          console.log('[SW]:', 'URL [' + urlString + '] from network');
        }

        return response;
      }

      // throw to reach the code in the catch below
      throw new Error("response is not ok");
    })
    // this needs to be in a catch() and not just in the then() above
    // cause if your network is down, the fetch() will throw
    ['catch'](function () {
      if (DEBUG) {
        console.log('[SW]:', 'URL [' + urlString + '] from cache if possible');
      }

      return cachesMatch(cacheUrl, CACHE_NAME);
    });
  }

  function handleNavigateFallback(fetching) {
    return fetching['catch'](function () {}).then(function (response) {
      if (!response || !response.ok) {
        if (DEBUG) {
          console.log('[SW]:', 'Loading navigation fallback [' + navigateFallbackURL + '] from cache');
        }

        return cachesMatch(navigateFallbackURL, CACHE_NAME);
      }

      return response;
    });
  }

  function mapAssets() {
    Object.keys(assets).forEach(function (key) {
      assets[key] = assets[key].map(function (path) {
        var url = new URL(path, location);

        if (externals.indexOf(path) === -1) {
          url.search = '';
        } else {
          // Remove hash from possible passed externals
          url.hash = '';
        }

        return url.toString();
      });
    });

    Object.keys(loadersMap).forEach(function (key) {
      loadersMap[key] = loadersMap[key].map(function (path) {
        var url = new URL(path, location);

        if (externals.indexOf(path) === -1) {
          url.search = '';
        } else {
          // Remove hash from possible passed externals
          url.hash = '';
        }

        return url.toString();
      });
    });

    hashesMap = Object.keys(hashesMap).reduce(function (result, hash) {
      var url = new URL(hashesMap[hash], location);
      url.search = '';

      result[hash] = url.toString();
      return result;
    }, {});

    externals = externals.map(function (path) {
      var url = new URL(path, location);
      url.hash = '';

      return url.toString();
    });
  }

  function addAllNormalized(cache, requests, options) {
    var allowLoaders = options.allowLoaders !== false;
    var bustValue = options && options.bust;
    var requestInit = options.request || {
      credentials: 'omit',
      mode: 'cors'
    };

    return Promise.all(requests.map(function (request) {
      if (bustValue) {
        request = applyCacheBust(request, bustValue);
      }

      return fetch(request, requestInit);
    })).then(function (responses) {
      if (responses.some(function (response) {
        return !response.ok;
      })) {
        return Promise.reject(new Error('Wrong response status'));
      }

      var extracted = [];
      var addAll = responses.map(function (response, i) {
        if (allowLoaders) {
          extracted.push(extractAssetsWithLoaders(requests[i], response));
        }

        return cache.put(requests[i], response);
      });

      if (extracted.length) {
        (function () {
          var newOptions = copyObject(options);
          newOptions.allowLoaders = false;

          var waitAll = addAll;

          addAll = Promise.all(extracted).then(function (all) {
            var extractedRequests = [].concat.apply([], all);

            if (requests.length) {
              waitAll = waitAll.concat(addAllNormalized(cache, extractedRequests, newOptions));
            }

            return Promise.all(waitAll);
          });
        })();
      } else {
        addAll = Promise.all(addAll);
      }

      return addAll;
    });
  }

  function extractAssetsWithLoaders(request, response) {
    var all = Object.keys(loadersMap).map(function (key) {
      var loader = loadersMap[key];

      if (loader.indexOf(request) !== -1 && loaders[key]) {
        return loaders[key](response.clone());
      }
    }).filter(function (a) {
      return !!a;
    });

    return Promise.all(all).then(function (all) {
      return [].concat.apply([], all);
    });
  }

  function matchCacheMap(request) {
    var urlString = request.url;
    var url = new URL(urlString);

    var requestType = undefined;

    if (request.mode === 'navigate') {
      requestType = 'navigate';
    } else if (url.origin === location.origin) {
      requestType = 'same-origin';
    } else {
      requestType = 'cross-origin';
    }

    for (var i = 0; i < cacheMaps.length; i++) {
      var map = cacheMaps[i];

      if (!map) continue;
      if (map.requestTypes && map.requestTypes.indexOf(requestType) === -1) {
        continue;
      }

      var newString = undefined;

      if (typeof map.match === 'function') {
        newString = map.match(url, request);
      } else {
        newString = urlString.replace(map.match, map.to);
      }

      if (newString && newString !== urlString) {
        return newString;
      }
    }
  }
}

function cachesMatch(request, cacheName) {
  return caches.match(request, {
    cacheName: cacheName
  })
  // Return void if error happened (cache not found)
  ['catch'](function () {});
}

function applyCacheBust(asset, key) {
  var hasQuery = asset.indexOf('?') !== -1;
  return asset + (hasQuery ? '&' : '?') + '__uncache=' + encodeURIComponent(key);
}

function getClientsURLs() {
  if (!self.clients) {
    return Promise.resolve([]);
  }

  return self.clients.matchAll({
    includeUncontrolled: true
  }).then(function (clients) {
    if (!clients.length) return [];

    var result = [];

    clients.forEach(function (client) {
      var url = new URL(client.url);
      url.search = '';
      url.hash = '';
      var urlString = url.toString();

      if (!result.length || result.indexOf(urlString) === -1) {
        result.push(urlString);
      }
    });

    return result;
  });
}

function isNavigateRequest(request) {
  return request.mode === 'navigate' || request.headers.get('Upgrade-Insecure-Requests') || (request.headers.get('Accept') || '').indexOf('text/html') !== -1;
}

function copyObject(original) {
  return Object.keys(original).reduce(function (result, key) {
    result[key] = original[key];
    return result;
  }, {});
}

function logGroup(title, assets) {
  console.groupCollapsed('[SW]:', title);

  assets.forEach(function (asset) {
    console.log('Asset:', asset);
  });

  console.groupEnd();
}
      WebpackServiceWorker(__wpo, {
loaders: {},
cacheMaps: [],
});
      module.exports = __webpack_require__(0)
    

/***/ })
/******/ ]);